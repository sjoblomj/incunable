<li>
  <label for="thumbnail_${num}"><img src="${small}" title="${title}" alt="${title}" /></label>
  <input type="radio" id="thumbnail_${num}" name="gallery" />
  <div class="thumbnail_overlay">
    <label for="close_${id}" title="Click to close"><img src="${large}" alt="${title}" class="large" /></label>
  </div>
</li>
